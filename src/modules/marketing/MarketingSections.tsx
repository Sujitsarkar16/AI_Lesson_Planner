import React from 'react';
import { motion } from 'framer-motion';
import { Link } from 'react-router-dom';
import AmbientCanvas from '@/modules/marketing/AmbientCanvas';

export type Feature = { icon: string; title: string; detail: string };
export type Faq = { question: string; answer: string };

export const SectionHeading: React.FC<{ eyebrow: string; title: string; copy?: string; align?: 'left' | 'center' }> = ({ eyebrow, title, copy, align = 'left' }) => <div className={align === 'center' ? 'mx-auto max-w-3xl text-center' : 'max-w-2xl'}><span className="eyebrow">{eyebrow}</span><h2 className="mt-4 text-3xl font-extrabold font-display tracking-[-.035em] text-brand-black sm:text-5xl">{title}</h2>{copy && <p className="mt-5 text-lg leading-8 text-slate-600">{copy}</p>}</div>;

export const PageHero: React.FC<{ eyebrow: string; title: React.ReactNode; copy: string; primary?: { label: string; to: string }; secondary?: { label: string; to: string }; children?: React.ReactNode; centered?: boolean }> = ({ eyebrow, title, copy, primary, secondary, children, centered = false }) => <section className="marketing-hero relative overflow-hidden px-5 py-20 lg:px-12 lg:py-28"><AmbientCanvas /><motion.div className={`relative mx-auto max-w-6xl ${children ? 'grid items-center gap-14 lg:grid-cols-[1fr_.9fr]' : ''}`} initial={{ opacity: 0, y: 22 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .65, ease: 'easeOut' }}><div className={centered ? 'mx-auto max-w-4xl text-center' : 'max-w-2xl'}><span className="eyebrow">{eyebrow}</span><h1 className="mt-6 text-4xl font-extrabold font-display tracking-[-.055em] text-brand-black sm:text-6xl lg:text-7xl">{title}</h1><p className={`mt-6 text-lg leading-8 text-slate-600 sm:text-xl ${centered ? 'mx-auto max-w-2xl' : 'max-w-xl'}`}>{copy}</p>{(primary || secondary) && <div className={`mt-9 flex flex-col gap-3 sm:flex-row ${centered ? 'justify-center' : ''}`}>{primary && <Link to={primary.to} className="material-button material-primary">{primary.label}<span className="material-symbols-outlined text-lg">arrow_forward</span></Link>}{secondary && <Link to={secondary.to} className="material-button material-secondary">{secondary.label}</Link>}</div>}</div>{children}</motion.div></section>;

export const FeatureGrid: React.FC<{ features: Feature[]; columns?: 2 | 3 }> = ({ features, columns = 3 }) => <div className={`mt-10 grid gap-5 ${columns === 2 ? 'md:grid-cols-2' : 'md:grid-cols-3'}`}>{features.map((feature, index) => <motion.article key={feature.title} className="soft-card rounded-3xl p-7" initial={{ opacity: 0, y: 18 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true, amount: .2 }} transition={{ delay: index * .06 }}><span className="feature-icon"><span className="material-symbols-outlined">{feature.icon}</span></span><h3 className="mt-5 text-xl font-extrabold text-brand-black">{feature.title}</h3><p className="mt-3 leading-7 text-slate-600">{feature.detail}</p></motion.article>)}</div>;

export const ProductMockup: React.FC<{ mode?: 'lesson' | 'team' }> = ({ mode = 'lesson' }) => <motion.div className="product-preview rounded-[1.75rem] p-3" initial={{ opacity: 0, scale: .97, y: 18 }} animate={{ opacity: 1, scale: 1, y: 0 }} transition={{ delay: .18, type: 'spring', stiffness: 130, damping: 20 }}><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="flex items-center justify-between border-b border-slate-100 px-5 py-4"><div className="flex items-center gap-2"><span className="size-2 rounded-full bg-primary"/><span className="text-sm font-extrabold">{mode === 'lesson' ? 'Lesson workspace' : 'School workspace'}</span></div><span className="rounded-full bg-brand-green px-3 py-1 text-xs font-bold text-emerald-700">{mode === 'lesson' ? 'Ready to review' : '3 shared templates'}</span></div><div className="grid min-h-[340px] grid-cols-[72px_1fr]"><aside className="border-r border-slate-100 bg-slate-50 p-3"><div className="mx-auto flex size-10 items-center justify-center rounded-xl bg-brand-black text-white"><span className="material-symbols-outlined text-lg">auto_stories</span></div><div className="mt-6 space-y-3">{['home','description','tune','ios_share'].map((icon, i) => <span key={icon} className={`mx-auto flex size-9 items-center justify-center rounded-lg ${i === 1 ? 'bg-primary text-white' : 'text-slate-400'}`}><span className="material-symbols-outlined text-lg">{icon}</span></span>)}</div></aside><div className="p-5"><p className="text-xs font-bold uppercase tracking-[.14em] text-primary">{mode === 'lesson' ? 'Year 8 · Science' : 'Science department'}</p><h3 className="mt-2 text-xl font-extrabold">{mode === 'lesson' ? 'Photosynthesis: energy in action' : 'Planning library'}</h3><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl bg-brand-yellow p-4"><span className="material-symbols-outlined text-primary">target</span><p className="mt-5 text-sm font-extrabold">{mode === 'lesson' ? 'Learning objectives' : 'Shared standards'}</p><div className="mt-3 h-2 rounded-full bg-white/80"/><div className="mt-2 h-2 w-3/4 rounded-full bg-white/80"/></div><div className="rounded-2xl bg-brand-blue p-4"><span className="material-symbols-outlined text-primary">route</span><p className="mt-5 text-sm font-extrabold">{mode === 'lesson' ? 'Learning sequence' : 'Team templates'}</p><div className="mt-3 h-2 rounded-full bg-white/80"/><div className="mt-2 h-2 w-2/3 rounded-full bg-white/80"/></div></div><div className="mt-3 flex items-center justify-between rounded-2xl border border-slate-200 p-4"><div><p className="text-sm font-extrabold">{mode === 'lesson' ? 'Differentiate this lesson' : 'Review before sharing'}</p><p className="mt-1 text-xs text-slate-500">Keep the learning goal in view</p></div><span className="flex size-9 items-center justify-center rounded-lg bg-brand-black text-white"><span className="material-symbols-outlined text-lg">arrow_forward</span></span></div></div></div></div></motion.div>;

export const Steps: React.FC<{ items: { number: string; title: string; detail: string }[] }> = ({ items }) => <div className="mt-12 grid gap-8 md:grid-cols-3">{items.map((item, index) => <div key={item.title} className="relative"><span className="step-number">{item.number}</span>{index < items.length - 1 && <span className="step-line hidden md:block"/>}<h3 className="mt-5 text-xl font-extrabold">{item.title}</h3><p className="mt-3 leading-7 text-slate-600">{item.detail}</p></div>)}</div>;

export const FAQ: React.FC<{ items: Faq[] }> = ({ items }) => { React.useEffect(() => { const script = document.createElement('script'); script.type = 'application/ld+json'; script.dataset.seoFaq = 'true'; script.text = JSON.stringify({ '@context': 'https://schema.org', '@type': 'FAQPage', mainEntity: items.map(({ question, answer }) => ({ '@type': 'Question', name: question, acceptedAnswer: { '@type': 'Answer', text: answer } })) }); document.head.querySelector('script[data-seo-faq="true"]')?.remove(); document.head.appendChild(script); return () => script.remove(); }, [items]); return <div className="mt-10 divide-y divide-slate-200 overflow-hidden rounded-3xl border border-slate-200 bg-white">{items.map((item) => <details key={item.question} className="faq-item group px-6 py-5 sm:px-8"><summary className="flex cursor-pointer list-none items-center justify-between gap-5 font-extrabold text-brand-black">{item.question}<span className="material-symbols-outlined text-primary transition group-open:rotate-45">add</span></summary><p className="max-w-3xl pt-4 leading-7 text-slate-600">{item.answer}</p></details>)}</div>; };

export const FinalCTA: React.FC<{ eyebrow?: string; title: string; copy: string; action: string; to: string; secondary?: { label: string; to: string } }> = ({ eyebrow = 'Start with one lesson', title, copy, action, to, secondary }) => <section className="px-5 py-20 lg:px-12"><div className="cta-panel relative mx-auto max-w-6xl overflow-hidden rounded-[2rem] bg-brand-black px-7 py-14 text-center text-white sm:px-14 sm:py-20"><span className="cta-orb"/><span className="eyebrow bg-white/10 text-white">{eyebrow}</span><h2 className="relative mx-auto mt-5 max-w-3xl text-3xl font-extrabold font-display tracking-[-.04em] sm:text-5xl">{title}</h2><p className="relative mx-auto mt-5 max-w-2xl text-lg leading-8 text-slate-300">{copy}</p><div className="relative mt-8 flex flex-col justify-center gap-3 sm:flex-row"><Link to={to} className="material-button bg-white text-brand-black">{action}<span className="material-symbols-outlined text-lg">arrow_forward</span></Link>{secondary && <Link to={secondary.to} className="material-button border border-white/20 bg-white/10 text-white">{secondary.label}</Link>}</div></div></section>;
